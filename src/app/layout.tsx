import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Text to Audio - Convert Text to Speech",
  description: "Convert your text to high-quality speech audio with customizable voice settings. Play, download, and enjoy natural-sounding text-to-speech conversion.",
  keywords: ["text to speech", "TTS", "audio conversion", "voice synthesis", "speech generation"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50">
          {children}
        </div>
      </body>
    </html>
  );
}